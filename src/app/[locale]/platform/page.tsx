export default function PlatformHome(): React.JSX.Element {
  return (
    <main className="p-6">
      <h1 className="text-xl font-semibold">Tenants</h1>
      <p className="mt-2 text-[var(--color-text-muted)]">
        Operator console. Phase 3e — provisioning, health, impersonation.
      </p>
    </main>
  );
}
